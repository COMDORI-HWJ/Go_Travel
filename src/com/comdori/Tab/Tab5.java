package com.comdori.Tab;

import javax.swing.*;
import com.teamdev.jxbrowser.chromium.Browser;
import com.teamdev.jxbrowser.chromium.swing.BrowserView;
import java.awt.*;

public class Tab5 extends JPanel {


    public Tab5() {
        setLayout(null);
        setBounds(0, 0, 1226, 662);

        /* 				브라우저			 */
        /*
        Browser browser = new Browser();
        BrowserView browserView = new BrowserView(browser);
        browserView.setLocation(0, 0);
        add(browserView, BorderLayout.CENTER);
        browserView.setSize(1181, 658);
        browser.loadURL("https://translate.google.com/");  //구글번역 URL
        //추후 API를 파싱하여 프로그램내에서 번역할 예정.

         */
    }

}
